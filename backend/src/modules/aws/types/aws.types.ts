export type AwsUploadFileType = 'image' | 'video';

export enum UploadDomain {
  GENERAL = 'general',
  TEST = 'test',
  PROFILE = 'profile',
}

export enum UploadKind {
  IMAGE = 'image',
  VIDEO = 'video',
}
